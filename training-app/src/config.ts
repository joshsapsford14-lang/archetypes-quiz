// Working title — change this single constant to rebrand the whole app.
export const APP_NAME = "Touchline";

// Default daily XP goal the Home progress ring fills toward (tunable).
export const DAILY_XP_GOAL = 120;
